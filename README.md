# 2048 Game

Implementations for playing 2048 game through text-based and graphical interfaces.

The text-based version can be played by running ```play_2048.py``` as main.

The graphical user interface version can be played by running ```play_2048_gui.py``` as main. This version requires that the pygame module is downloaded.